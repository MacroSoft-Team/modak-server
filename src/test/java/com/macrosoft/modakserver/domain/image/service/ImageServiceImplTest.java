package com.macrosoft.modakserver.domain.image.service;

import org.junit.jupiter.api.Nested;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.transaction.annotation.Transactional;

@SpringBootTest
@Transactional
class ImageServiceImplTest {


    @Nested
    class GetImageDetailTests {
        @Test
        void 이미지_상세_조회_한다() {
            // given
            // when
            // then
        }

        @Test
        void 모닥불에_속한_멤버가_아니면_예외발생() {
            // given
            // when
            // then
        }

        @Test
        void 모닥불에_속한_이미지_아니면_예외발생() {
            // given
            // when
            // then
        }

        @Test
        void 이미지_아이디가_없으면_예외발생() {
            // given
            // when
            // then
        }
    }

    @Nested
    class RemoveImagesTests {
        @Test
        void 이미지_삭제_한다() {
            // given
            // when
            // then
        }

        @Test
        void 모닥불에_속한_멤버가_아니면_예외발생() {
            // given
            // when
            // then
        }

        @Test
        void 모닥불에_속한_이미지_아니면_예외발생() {
            // given
            // when
            // then
        }

        @Test
        void 이미지_아이디가_없으면_예외발생() {
            // given
            // when
            // then
        }
    }
}